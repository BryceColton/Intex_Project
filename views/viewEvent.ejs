<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Event Details</title>
    <link rel="icon" type="image/png" href="/img/logo.png" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link rel="stylesheet" href="/styles.css" />
    <link
      href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <nav class="navbar">
        <div class="navbar-logo">Turtle Shelter Project</div>
        <div class="navbar-buttons">
            <!-- These will be navbar buttons to take the admin to pages that will manage the volunteers and events -->
            <form action="/admin" method="GET"><button class="btn" type="submit">Admin Home</button></form>
            <form action="/manageEvents" method="GET"><button class="btn" type="submit">Manage Events</button></form>
        </nav>

        <!-- Event Info Here -->
    <form action="handlePendingEvent/:<%= event.eventid %>" method="POST">

        <p><%= event.event_name %></p>
        
        <label for="status">Approve or Decline?</label>
              <!-- Approve/Decline (Radio Buttons) -->
      <label>
        <input type="radio" name="status" value="A" <%= event.status === '' ? 'checked' : ''
        %>> Approve
      </label>
      <label>
        <input type="radio" name="status" value="D" <%= event.status === 'B' ? 'checked' : ''
        %>> Decline
      </label>
  
        <button type="submit">Finish</button>
    </form>

    
  </body>
</html>
