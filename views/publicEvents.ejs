<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Public Events</title>
</head>
<body>
  <h1>Public Events</h1>

  <% if (events && events.length > 0) { %> <!-- Check if events is defined and has elements -->
    <table border="1">
      <thead>
        <tr>
          <th>Event Name</th>
          <th>Date</th>
          <th>Address</th>
          <th>City</th>
          <th>State</th>
          <th>Zip</th>
        </tr>
      </thead>
      <tbody>
        <% events.forEach(event => { %>
          <tr>
            <td><%= event.event_name %></td>
            <td><%= new Date(event.date).toLocaleDateString() %></td>
            <td><%= event.event_address %></td>
            <td><%= event.city %></td>
            <td><%= event.state %></td>
            <td><%= event.zip %></td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  <% } else { %>
    <p>No events available.</p>
  <% } %>
</body>
</html>
