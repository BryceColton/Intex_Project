<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Admin -- Turtle Shelter Project</title>
    <link rel="icon" type="image/png" href="/img/logo.png" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link rel="stylesheet" href="/styles.css" />
    <link
      href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap"
      rel="stylesheet"
    />
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700;900&display=swap" rel="stylesheet">

    <style>
       body {
        font-family: 'Roboto', sans-serif;
        margin: 0;
        padding: 0;
        background-color: #f8f9fa;
      }
    
      h1 {
        text-align: center;
        margin-top: 30px;
        color: #424242;
        font-size: 2em;
      }
    
      /* Table Styles */
      table {
        width: 100%;
        margin: 30px auto;
        border-collapse: collapse;
        background-color: #ffffff;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        border-radius: 8px;
        overflow: hidden;
        table-layout: fixed; /* Ensure columns don't overflow */
      }
    
      th, td {
        padding: 12px 20px;
        text-align: left;
        border-bottom: 1px solid #ddd;
        word-wrap: break-word; /* Prevent text from overflowing */
      }
    
      th {
        background-color: #424242;
        color: white;
      }
    
      td {
        background-color: #f1f1f1;
      }

            /* Target the 6th column (Delete) */
  th:nth-child(8), td:nth-child(8) {
    width: 120px; /* Make the 6th column a bit wider */
    text-align: center; /* Center align the buttons */
  }

  /* Target the 4th column (Last Name) */
  th:nth-child(9), td:nth-child(9) {
    width: 80px; /* Reduce the width of the Last Name column */
  }
  
    
      tr:hover td {
        background-color: #424242;
        color: white;
      }
    
      /* Container for table to enable scrolling on smaller screens */
      .table-container {
        overflow-x: auto; /* Enables horizontal scrolling */
        -webkit-overflow-scrolling: touch; /* Smooth scrolling on mobile */
        width: 100%;
        margin-bottom: 30px; /* To separate the table from the content below */
      }
    
      /* Buttons inside the table */
      .btn-table {
        padding: 8px 15px;
        background-color: #424242;
        color: white;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        font-size: 0.9em;
        transition: background-color 0.3s;
      }
    
      .btn-table:hover {
        background-color: white;
        color: #424242;
      }
    
      .btn-delete {
        background-color: #e74a3b;
        margin-left: 5px;
      }
    
      .btn-delete:hover {
        background-color: #c0392b;
      }
    
      /* Responsive Design */
      @media (max-width: 768px) {
        table {
          width: 100%;
          overflow-x: auto;
        }
    
        th, td {
          font-size: 0.9em; /* Adjust font size for smaller screens */
          padding: 8px;
        }
    
        .form-container button {
          width: 100%; /* Makes the form button more responsive */
        }
      }
    
      /* Form Styles */
      .form-container {
        text-align: center;
        margin-top: 30px;
      }
    
      .form-container button {
        padding: 10px 20px;
        background-color: #424242;
        color: white;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        font-weight: 600;
        font-size: 1em;
        transition: background-color 0.3s;
      }
    

      .bottom-buttons {
        display: flex;
        justify-content: center; /* Center the buttons horizontally */
        gap: 20px; /* Add spacing between buttons */
        margin-top: 30px;
        padding: 20px 0;

    }
    </style>
  </head>
  <body>
    <nav class="navbar">
        <div class="navbar-logo">Turtle Shelter Project</div>
        <div class="navbar-buttons">
          <!-- These will be navbar buttons to take the admin to pages that will manage the volunteers and events -->
          <form action="/admin" method="GET"><button class="btn" type="submit">Admin Home</button></form>
          <form action="/manageUsers" method="GET"><button class="btn" type="submit">Manage Admin Users</button></form>
          <form action="/manageVolunteers" method="GET"><button class="btn" type="submit">Manage Volunteers</button></form>
          <form action="/manageEvents" method="GET"><button class="btn" type="submit">Manage Events</button></form>
        </nav>

    <!-- Event Info Here -->
    <main style="display: flex; justify-content: center; align-items: center">
      <section class="form-section">
        <form action="/viewCompletedEvent/<%= event.eventid %>" method="POST">
          <h2 class="form-title">Event Details</h2>
          <fieldset>
            <legend>Requester Details</legend>
            <div class="form-group">
              <label for="event_name">Event Name: <b><%= event.event_name %></b></label>
            </div>
            <div class="form-group">
              <label for="organization">Organization: <b><%= event.organization %></b></label>
            </div>
            <div class="form-group">
              <label for="organizer_first_name"
                >Organizer First Name: <b><%= event.organizer_first_name %></b></label
              >
            </div>
            <div class="form-group">
              <label for="organizer_last_name"
                >Organizer Last Name: <b><%= event.organizer_last_name %></b></label
              >
            </div>
            <div class="form-group">
              <label for="org_phone">Phone Number: <b><%= event.org_phone %></b></label>
            </div>
            <div class="form-group">
              <label for="org_email">Email: <b><%= event.org_email %></b></label>
            </div>
          </fieldset>

          <fieldset>
            <legend>Event Details</legend>
            <div class="form-group">
              <label for="date">Date: <b><%= event.date %></b></label>
            </div>
            <div class="form-group">
              <label for="event_address"
                >Event Address:
                <b
                  ><%= event.event_address %>, <%= event.city %>, <%= event.state %>, <%= event.zip
                  %></b
                ></label
              >
            </div>
            <div class="form-group">
              <label for="event_type">Event Type: <b><%= event.event_type %></b></label>
            </div>
            <div class="form-group">
              <label>Public: <b><%= event.public == 'true' ? 'Yes' : 'No' %></b></label>
            </div>
            <div class="form-group">
              <label
                >Guest Speaker: <b><%= event.guest_speaker == 'true' ? 'Yes' : 'No' %></b></label
              >
            </div>
            <div class="form-group">
              <label for="numexpected"
                >Expected Number of People: <b><%= event.numexpected %></b></label
              >
            </div>
            <div class="form-group">
              <label for="duration">Duration: <b><%= event.duration %> hours</b></label>
            </div>
            <div class="form-group">
              <label for="venuedescription"
                >Venue Description: <b><%= event.venuedescription %></b></label
              >
            </div>
          </fieldset>

          <fieldset>
            <legend>Event Numbers</legend>
            <div class="form-group">
              <table>
                <tbody>
                  <tr>
                    <td>Total Number of Attendees:</td>
                    <td><b><%= event.num_actual %></b></td>
                  </tr>
                  <tr>
                    <td>Total Number of Pockets:</td>
                    <td><b><%= event.num_pocket %></b></td>
                  </tr>
                  <tr>
                    <td>Total Number of Collars:</td>
                    <td><b><%= event.num_collar %></b></td>
                  </tr>
                  <tr>
                    <td>Total Number of Envelopes:</td>
                    <td><b><%= event.num_envelopes %></b></td>
                  </tr>
                  <tr>
                    <td>Total Number of Vests:</td>
                    <td><b><%= event.num_vests %></b></td>
                  </tr>
                  <tr>
                    <td>Total Number of Completed Products:</td>
                    <td><b><%= event.num_completed %></b></td>
                  </tr>
                  <tr>
                    <td>Total Number of Vests Distributed:</td>
                    <td><b><%= event.num_distributed == null ? 0 : event.num_distributed%></b></td>
                  </tr>
                </tbody>
              </table>
            </div>
          </fieldset>
        </form>
      </section>
    </main>
    <br><br>
    <form action="/adminCompletedEvents" method="GET">
      <button class="btn-table" type="submit">Back to Completed Events</button>
    </form>
  </body>
</html>
